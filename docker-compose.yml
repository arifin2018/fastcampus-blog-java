services:

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    volumes:
      - ./docker/mysql:/var/lib/mysql
    ports:
      - 3306:3306
  # phpmyadmin:
  #   image: phpmyadmin
  #   restart: always
  #   ports:
  #     - 3307:80
  #   environment:
  #     - PMA_ARBITRARY=1  